class dbExemple{
    constructor(){
        // Je sais pas quoi mettre ici
    }

    getAllUsers(){
        // return users;
        // Ici mettre l'appelle a la db pour le getallusers, dans le controller, initialiser la classe dbExemple et appeller cette methode pour get all users
        // genre: db = new dbExemple()
        // db.getAllUsers
        // NE PAS OUBLIER DE RETURN L'OBJET USER ET NON PAS JUSTE LES DONNEES (un tableau d'objet user en l'occurence)
    }

    getUserById(id){
        // Mettre ce qu'il faut, etc..
        //return id;
    }
}